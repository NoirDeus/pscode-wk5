// Exercise 1: Calculate the Sum of Two Numbers

a + b = c

num1 + num2 = sum
print sum

ask for 2 numbers 
add the 2 number together 
print on the screen the sum of those 2 numbers


// Exercise 2: Check if a Number is Even or Odd

var Number
var Test

ask for a number
Number=input

Number % 2 = Test

If Test = 0 
	echo "Even"
else
	echo "Odd" 




// Exercise 3: 
Write pseudo code that will perform the following.    

a) Read in 5 separate numbers.   
b) Calculate the average of the five numbers.
c) Find the smallest (minimum) and largest (maximum) of the five entered numbers.
d) Write out the results found from steps b and c with a message describing what they are

var Num1
var Num2
var Num3
var Num4
var Num5
var NumSum
var TotNum
var NumAve
var NumMin
var NumMax

echo "insert first number"
Num1=entered text

echo "insert second number"
Num2=entered text

echo "insert third number"
Num3=entered text

echo "insert fourth number"
Num4=entered text

echo "insert fifth number"
Num5=entered text

TotNum=number of entered numbers

NumSum=Num1+Num2+Num3+Num4=Num5

NumAve=NumSum/TotNum


If Num1 > Num2
	then Num1 = NumMax
Else 
	Num2 = Num Max

If Num2 > Num3
	then Num2 = NumMax
Else
	Num3 = NumMax

If Num3 > Num4
	then Num3 = NumMax
Else
	Num4 = NumMax

If Num4 > Num5
	then Num4 = NumMax
Else
	Num5 = NumMax


If Num1 < Num2
	then Num1 = NumMin
Else 
	Num2 = Num Min

If Num2 < Num3
	then Num2 = NumMin
Else
	Num3 = NumMin

If Num3 < Num4
	then Num3 = NumMin
Else
	Num4 = NumMin

If Num4 < Num5
	then Num4 = NumMin
Else
	Num5 = NumMin



Echo "The average of your entered numbers is"
Echo NumAve

Echo "The smallest number entered is"
Echo NumMin

Echo "The largest number entered is"
Echo NumMax


















-- END --